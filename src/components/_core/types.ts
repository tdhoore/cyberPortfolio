export interface pageBase {
  pageCompName?: string;
  match?: any;
  routeAnimating?: boolean;
  test?: any;
}

export interface headerProps {
  title: string;
  url: string;
  image?: string;
}